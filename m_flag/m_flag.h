#ifndef M_FLAG_H
#define M_FLAG_H

/*
 * Prints metadata (owner, group, permissions, etc.) for each entity in the archive.
 */
void print_metadata_from_archive(const char *archive_name);

#endif // M_FLAG_H
